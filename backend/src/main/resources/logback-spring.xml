<?xml version="1.0" encoding="UTF-8"?>
<!--
  Briefen — Logback configuration with switchable log format.

  Set BRIEFEN_LOG_FORMAT=json to emit structured JSON logs (one JSON object per line),
  useful for log aggregation tools like Loki, Grafana, Datadog, or simple jq filtering.

  Default (BRIEFEN_LOG_FORMAT unset or any other value): human-readable console output
  matching Spring Boot's built-in pattern.
-->
<configuration>

    <!-- Import Spring Boot's default colour-conversion rules and patterns -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <!-- ── Human-readable console output (default) ── -->
    <springProfile name="!json">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>${CONSOLE_LOG_PATTERN:-%clr(%d{${LOG_DATEFORMAT_PATTERN:-yyyy-MM-dd'T'HH:mm:ss.SSSXXX}}){faint} %clr(${LOG_LEVEL_PATTERN:-%5p}) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%15.15t]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}}</pattern>
            </encoder>
        </appender>

        <root level="${BRIEFEN_LOG_LEVEL:-INFO}">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <!-- ── Structured JSON output (BRIEFEN_LOG_FORMAT=json) ── -->
    <springProfile name="json">
        <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <includeCallerData>false</includeCallerData>
                <timestampPattern>yyyy-MM-dd'T'HH:mm:ss.SSSXXX</timestampPattern>
                <fieldNames>
                    <timestamp>timestamp</timestamp>
                    <version>[ignore]</version>
                    <levelValue>[ignore]</levelValue>
                </fieldNames>
            </encoder>
        </appender>

        <root level="${BRIEFEN_LOG_LEVEL:-INFO}">
            <appender-ref ref="JSON"/>
        </root>
    </springProfile>

</configuration>
