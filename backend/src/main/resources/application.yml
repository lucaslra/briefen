spring:
  application:
    name: briefen
  config:
    # Loads .env file if present (KEY=VALUE format). No-op when missing (CI/Docker use real env vars).
    import: optional:file:.env[.properties]
  data:
    mongodb:
      uri: ${BRIEFEN_MONGODB_URI}
      auto-index-creation: true

server:
  port: ${BRIEFEN_SERVER_PORT:8080}

ollama:
  base-url: ${BRIEFEN_OLLAMA_URL:http://localhost:11434}
  model: ${BRIEFEN_OLLAMA_MODEL:gemma3:4b}
  timeout: 300s

openai:
  base-url: https://api.openai.com
  timeout: 120s

article:
  fetch-timeout: 10s

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always
